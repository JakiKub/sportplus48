import { useNavigate } from "react-router-dom"

const ArenaTA = () => {
    const navigate = useNavigate();

    return (
        <section id="arenaTA">

        </section>
    )
}

export default ArenaTA